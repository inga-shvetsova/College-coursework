dividend = float(input("Input the dividend: "))
divisor = float(input("Input the divisor: "))

quatient = dividend // divisor
remainder = dividend % divisor

sum = quatient + remainder

print("The quotient + the remainder is " + str(sum))